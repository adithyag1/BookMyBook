const urls= {
    HOME: '/',
    LOGIN: '/login',
    DASHBOARD: '/dashboard',
    PROFILE: '/profile'
}

const routes={
    HOME: urls.HOME,
    LOGIN: urls.LOGIN,
    DASHBOARD: urls.DASHBOARD,
    PROFILE: urls.PROFILE
}

export default routes;